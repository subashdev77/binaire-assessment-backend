services:
  - type: web
    name: binaire-assessment-backend
    runtime: node
    buildCommand: npm ci
    startCommand: node dist/server.js
    healthCheckPath: /api/queue
    envVars:
      - key: NODE_VERSION
        value: "20"
